{{/* login/email - Email login */}}
{{ define "login/email" }}
{{ template "layout" . }}
{{ end }}

{{ define "content" }}
<div class="max-w-md mx-auto bg-white dark:bg-gray-800 rounded-lg shadow p-8">
    <h1 class="text-2xl font-bold mb-6 text-center">Email Login</h1>
    <p class="text-gray-600 dark:text-gray-400 mb-6 text-center text-sm">We'll send you a code to verify your email</p>

    {{ if .Error }}
    <div class="bg-red-100 dark:bg-red-900 text-red-700 dark:text-red-200 p-3 rounded mb-4">{{ .Error }}</div>
    {{ end }}

    <form method="POST" class="space-y-4">
        <div>
            <label for="email" class="block text-sm font-medium mb-1">Email</label>
            <input type="email" id="email" name="email" value="{{ .Email }}" required
                class="w-full px-3 py-2 border rounded dark:bg-gray-700 dark:border-gray-600">
        </div>
        <button type="submit" class="w-full bg-blue-600 text-white py-3 rounded hover:bg-blue-700">Send Code</button>
    </form>

    <p class="mt-4 text-center"><a href="/login" class="text-blue-600 hover:underline">Back to login options</a></p>
</div>
{{ end }}
